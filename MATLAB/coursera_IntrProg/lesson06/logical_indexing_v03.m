%% Example 
% This is what you do in MATLAB
% just with one line of code
% The elements of w are set equal to the element of
% v whereever those elements are greater than or equal
% to 0

w = v(v >= 0);